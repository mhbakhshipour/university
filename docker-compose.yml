version: '3'

networks:
  univercity:
    driver: bridge

services:
  mysql:
    image: mysql:5.7
    restart: always
    ports:
      - '3307:3306'
    environment:
      MYSQL_DATABASE: 'univercity'
      MYSQL_USER: 'univercity'
      MYSQL_PASSWORD: 'rMZew4dBbzHastXP'
      MYSQL_ROOT_PASSWORD: 'rMZew4dBbzHastXP'
    volumes:
      - mysql-data-volume:/var/lib/mysql
    networks:
      - univercity

  backend:
    build: .
    restart: always
    ports:
      - '8181:8000'
    command: bash -c "export environment=docker.env && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    depends_on:
      - mysql
    networks:
      - univercity

volumes:
  mysql-data-volume: